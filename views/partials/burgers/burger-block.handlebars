<li>
	{{burger_names}}

	<button class="change-devoured" data-id="{{this.id}}" data-eatNewBurger="{{devoured}}">
		{{#if devoured}}devoure{{else}}Order Burger{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">change-devoured!</button>
</li>